<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Account - ADOPT-A-PET</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar {
            display: flex;
            justify-content: space-between;
        }

        .navbar-brand {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .navbar-brand img {
            margin-right: 10px;
        }

        .active-link {
            background-color: #007bff;
            color: white;
        }

        .footer {
            position: fixed;
            left: 0;
            bottom: 0;
            width: 100%;
            background-color: black;
            color: white;
            text-align: center;
        }

        .time-display {
            font-weight: bold;
            font-size: 1.2em;
            margin-right: 20px;
        }

        /* Center the form and give it some style */
        .form-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 80vh;
            background: linear-gradient(135deg, #544f4f, #bec5c5);
        }

        .form-box {
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
            text-align: center;
        }

        .form-box h1 {
            margin-bottom: 20px;
            color: #007bff;
        }

        .form-box input {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            border: 1px solid #ddd;
        }

        .form-box button {
            width: 100%;
            padding: 10px;
            border-radius: 5px;
            border: none;
            color: white;
            font-size: 16px;
        }

        .form-box button[type="submit"] {
            background-color: #007bff;
            margin-bottom: 10px;
        }

        .form-box button[type="button"] {
            background-color: #6c757d;
        }

        .placeholder-img {
            margin-top: 20px;
            margin-bottom: 20px;
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <!-- Header -->
        <%- include('partials/header') %>

        <!-- Main layout: side menu and content area -->
        <div class="row">
            <!-- Side Menu -->
            <nav class="col-md-3 bg-light">
                <div class="list-group">
                    <a href="/home" class="list-group-item list-group-item-action">Home</a>
                    <a href="/createaccount" class="list-group-item list-group-item-action active-link">Create Account</a>
                    <a href="/find" class="list-group-item list-group-item-action">Find a dog/cat</a>
                    <a href="/dog" class="list-group-item list-group-item-action">Dog Care</a>
                    <a href="/cat" class="list-group-item list-group-item-action">Cat Care</a>
                    <a href="/giveaway" class="list-group-item list-group-item-action">Have a pet to give away</a>
                    <a href="/contactus" class="list-group-item list-group-item-action">Contact Us</a>
                </div>
            </nav>

            <!-- Content Area -->
            <main class="col-md-9">
                <div class="form-container">
                    <div class="form-box">
                        <h1>Create Account</h1>
                        <img src="/images/logo.jpg" alt="User Image" class="placeholder-img">
                        <form action="/createaccount" method="POST">
                            <div>
                                <input type="text" id="username" name="username" placeholder="Username" required>
                            </div>
                            <div>
                                <input type="password" id="password" name="password" placeholder="Password" required>
                            </div>
                            <button type="submit">Sign Up</button>
                            <button type="button" onclick="window.location.href='/login'">Sign In</button>
                        </form>
                        <p><%= message %></p>
                    </div>
                </div>
            </main>
        </div>

        <!-- Footer -->
        <%- include('partials/footer') %>
    </div>

    <script>
        function updateTime() {
            const now = new Date();
            const timeString = now.toLocaleTimeString();
            const dateString = now.toLocaleDateString();
            document.getElementById('time').innerHTML = dateString + ' ' + timeString;
        }
        setInterval(updateTime, 1000); 
        updateTime(); 
    </script>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
